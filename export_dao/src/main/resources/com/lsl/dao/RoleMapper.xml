<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lsl.dao.RoleMapper">
    <resultMap id="BaseResultMap" type="com.lsl.system.Role">
        <id column="role_id" property="id"/>
        <result column="name" property="name"/>
        <result column="remark" property="remark"/>
        <result column="order_no" property="orderNo"/>
        <result column="company_id" property="companyId"/>
        <result column="company_name" property="companyName"/>
    </resultMap>

    <!--查询全部-->
    <select id="findAll" resultMap="BaseResultMap">
        select * from pe_role
    </select>

    <!--根据id查询-->
    <select id="findById" parameterType="java.lang.String" resultMap="BaseResultMap">
        select * from pe_role where role_id = #{id}
    </select>

    <!--删除-->
    <delete id="delete">
        delete from pe_role where role_id = #{id}
    </delete>

    <!--保存-->
    <insert id="save">
        insert into pe_role (role_id, name, remark, order_no,company_id, company_name)
        values (#{id}, #{name}, #{remark}, #{orderNo}, #{companyId}, #{companyName})
    </insert>

    <!--更新-->
    <update id="update">
        update pe_role
        set name = #{name},
          remark = #{remark},
          order_no = #{orderNo}
        where role_id = #{id}
    </update>
    <select id="findModulesByRoleId" resultMap="BaseResultMap">
        select * from `ss_module` m inner join `pe_role_module`  rm on m.`module_id`=rm.`module_id` where rm.`role_id`=#{id}
    </select>

    <select id="findRoleByUserId" resultMap="BaseResultMap">
        select * from `pe_role` pr inner join `pe_role_user` pru on pr.role_id = pru.role_id where pru.user_id=#{id};

    </select>
</mapper>